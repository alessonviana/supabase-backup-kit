version: 2
updates:
  # Keep the third-party GitHub Actions this kit depends on patched.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
